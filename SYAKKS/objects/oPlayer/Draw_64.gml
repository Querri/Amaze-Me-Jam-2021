/// @desc draw health and items
/*
spriteWidth = sprite_get_width(sHeartIcon);
startX = (room_width / 2) * (spriteWidth * 2.5);
startY = sprite_get_height(sHeartIcon);

for (var hearts = 0; hearts < maxHearts; hearts++) {
	image = HEARTS[hearts, HEARTS.INDEX];
	xx = startX - (spriteWidth * hearts);
	draw_sprite(sHeartIcon, image, xx, startY);
}

draw_text(xx, startY, "BAA BAA");

*/